# Starter pipeline
# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml

trigger:
- develop
resources:
  repositories:
    - repository: pilot-infra
      type: github
      name: PilotInfra
stages: 
  - template: /pipelines/templates/build-service-template.yml@pilot-infra
    parameters:
      ServiceName: pilot-user-service

