apiVersion: v1
kind: Service
metadata:
  name: {{ include "pilotuserservice.fullname" . }}
  labels:
    {{- include "pilotuserservice.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pilotuserservice.selectorLabels" . | nindent 4 }}
