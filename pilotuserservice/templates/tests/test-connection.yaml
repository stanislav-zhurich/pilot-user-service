apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pilotuserservice.fullname" . }}-test-connection"
  labels:
    {{- include "pilotuserservice.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pilotuserservice.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
