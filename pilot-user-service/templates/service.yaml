apiVersion: v1
kind: Service
metadata:
  name: {{ include "pilot-user-service.fullname" . }}
  labels:
    {{- include "pilot-user-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pilot-user-service.selectorLabels" . | nindent 4 }}
