stages:
    - stage: test 
      jobs:
          - job: 
            steps:
            - bash: |
                fileExists=$([ -d "$(System.DefaultWorkingDirectory)" ])
                echo "##vso[task.setvariable variable=RUN_TERRAFORM]$fileExists"
            - task: CmdLine@2
              inputs:
                script: 'echo test $(RUN_TERRAFORM)'